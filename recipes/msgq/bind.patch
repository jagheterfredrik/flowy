--- a/msgq/impl_zmq.cc
+++ b/msgq/impl_zmq.cc
@@ -108,7 +108,7 @@ int ZMQPubSocket::connect(Context *context, std::string endpoint, bool check_end
     return -1;
   }

-  full_endpoint = "tcp://*:";
+  full_endpoint = "tcp://127.0.0.1:";
   if (check_endpoint){
     full_endpoint += std::to_string(get_port(endpoint));
   } else {